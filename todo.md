# Todo 👷

## Relelase 1 ✨

* Hide the notificaiton preferences on the user edit filament page if the user is not an administrator

* The draw start at should only be a date (not time). Same for the draw ends at. The start date must be in the future and the end date must be greater than the start date. The draw description is mandatory. The draws list in filament should default to the draw start date ascending

* on the front end draws page, the days remaing should be a whole number.

* if a draw is active, but passed the end date, hide the register tickets button and add a Draw a Winner button that will randomly select a winner among the tickets and update the draw accordinly, and change the status to draw finalized and the status to finalized

* if a draw is active or finalized, protect the draw details section, the schedule section, and the ticket pricing section. Remove the delete button on the edit draw, and the delete option on the draws list.

* move memberships link from the admin pages to the Fundraising section of the menu, before draws

* hide the status section on the edit draw. its not needed.

## Release 2 ⚡


* how does a draw become active